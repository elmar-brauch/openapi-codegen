# Demo project for Swagger Codegen

After checkout of this project, you have to generate model classes.
With eclipse the following Maven command should be executed to do so:
mvn clean package eclipse:eclipse